# 长期运维模板：
# 1. 复制为 compose.mysql.ops.yml
# 2. 替换下面所有密码/密钥占位值
# 3. 再执行 docker compose -f compose.mysql.ops.yml up --build -d

volumes:
  mysql_data:

services:
  app:
    container_name: vansour-image-mysql-app
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/vansour/vansour-image:latest
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
      dragonfly:
        condition: service_healthy
    environment:
      - REDIS_URL=redis://dragonfly:6379
      - JWT_SECRET=replace-with-a-random-secret-at-least-32-characters
      - AUTH_COOKIE_SECURE=true
      - AUTH_COOKIE_SAME_SITE=Strict
    volumes:
      - "./data-mysql:/data"
    restart: unless-stopped
    healthcheck:
      test: curl -f http://localhost:8080/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  mysql:
    container_name: vansour-image-mysql
    image: mysql:8.4
    environment:
      - MYSQL_DATABASE=image
      - MYSQL_USER=vansour_image
      - MYSQL_PASSWORD=replace-with-strong-app-password
      - MYSQL_ROOT_PASSWORD=replace-with-strong-root-password
    volumes:
      - "mysql_data:/var/lib/mysql"
    healthcheck:
      test: mysqladmin ping -h 127.0.0.1 -uvansour_image -preplace-with-strong-app-password
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  dragonfly:
    container_name: vansour-image-mysql-dragonfly
    image: ghcr.io/dragonflydb/dragonfly:latest
    command: --dir=/data
    healthcheck:
      test: redis-cli ping
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
