# 统一手工部署入口。
#
# 默认启用：PostgreSQL + Redis 8。
# 如果你要改成 MySQL / MariaDB / Dragonfly / 无外部缓存，直接在这个文件里替换或解注释对应片段即可。
#
# 建议切换顺序：
# 1. 先改 `services.app` 里的 `depends_on` / `environment` / `volumes`
# 2. 再把数据库服务从 `postgres` 换成你需要的 `mysql`
# 3. 最后决定 `cache` 是 Redis 8、Dragonfly，还是彻底去掉
# 3. 最后决定 `cache` 是 Redis 8、Dragonfly，还是彻底去掉
#
#
# 生产环境如果启用 MySQL / MariaDB 运维口令示例，记得替换：
# 生产环境如果启用 MySQL / MariaDB 运维口令示例，记得替换：
# - JWT_SECRET
# - JWT_SECRET
# - 数据库应用用户密码
# - 数据库应用用户密码
# - 数据库 root 密码
# - 数据库 root 密码


volumes:
volumes:
  postgres_data:
  postgres_data:
  mysql_data:
  mysql_data:
  mariadb_data:
  mariadb_data:


services:
services:
  app:
  app:
    container_name: avenrixa-app
    container_name: avenrixa-app
    build:
    build:
      context: .
      context: .
      dockerfile: Dockerfile
      dockerfile: Dockerfile
      args:
      args:
        APP_VERSION: ${APP_VERSION:-0.1.3-dev.0}
        APP_VERSION: ${APP_VERSION:-0.1.3-dev.0}
        APP_REVISION: ${APP_REVISION:-dev}
        APP_REVISION: ${APP_REVISION:-dev}
        BUILD_DATE: ${BUILD_DATE:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    image: ${APP_IMAGE_REF:-ghcr.io/vansour/avenrixa:latest}
    image: ${APP_IMAGE_REF:-ghcr.io/vansour/avenrixa:latest}
    ports:
    ports:
      - "8080:8080"
      - "8080:8080"
    depends_on:
    depends_on:
      postgres:
      postgres:
        condition: service_healthy
        condition: service_healthy
      cache:
      cache:
        condition: service_healthy
        condition: service_healthy
    environment:
    environment:
      DATABASE_KIND: postgresql
      DATABASE_KIND: postgresql
      DATABASE_URL: postgresql://user:pass@postgres:5432/image
      DATABASE_URL: postgresql://user:pass@postgres:5432/image
      REDIS_URL: redis://cache:6379
      REDIS_URL: redis://cache:6379
      JWT_SECRET: your-secret-key-change-in-production
      JWT_SECRET: your-secret-key-change-in-production
      AUTH_COOKIE_SECURE: "false"
      AUTH_COOKIE_SECURE: "false"
      AUTH_COOKIE_SAME_SITE: Strict
      AUTH_COOKIE_SAME_SITE: Strict
    volumes:
    volumes:
      - "./data:/data"
      - "./data:/data"
    restart: unless-stopped
    restart: unless-stopped
    healthcheck:
    healthcheck:
      test: curl -f http://localhost:8080/health || exit 1
      test: curl -f http://localhost:8080/health || exit 1
      interval: 30s
      interval: 30s
      timeout: 10s
      timeout: 10s
      retries: 3
      retries: 3
      start_period: 40s
      start_period: 40s


    # MySQL 8.4 开发 / 联调 / 烟测：
    # MySQL 8.4 开发 / 联调 / 烟测：
    # 1. 把上面的 `depends_on.postgres` 改成 `depends_on.mysql`
    # 1. 把上面的 `depends_on.postgres` 改成 `depends_on.mysql`
    # 2. 把环境变量和卷改成下面这样
    # 2. 把环境变量和卷改成下面这样
    #
    #
    # depends_on:
    # depends_on:
    #   mysql:
    #   mysql:
    #     condition: service_healthy
    #     condition: service_healthy
    #   cache:
    #   cache:
    #     condition: service_healthy
    #     condition: service_healthy
    # environment:
    # environment:
    #   DATABASE_KIND: mysql
    #   DATABASE_KIND: mysql
    #   DATABASE_URL: mysql://user:pass@mysql:3306/image
    #   DATABASE_URL: mysql://user:pass@mysql:3306/image
    #   REDIS_URL: redis://cache:6379
    #   REDIS_URL: redis://cache:6379
    #   JWT_SECRET: your-secret-key-change-in-production
    #   JWT_SECRET: your-secret-key-change-in-production
    #   AUTH_COOKIE_SECURE: "false"
    #   AUTH_COOKIE_SECURE: "false"
    #   AUTH_COOKIE_SAME_SITE: Strict
    #   AUTH_COOKIE_SAME_SITE: Strict
    # volumes:
    # volumes:
    #   - "./data-mysql:/data"
    #   - "./data-mysql:/data"


    # MariaDB 12 开发 / 联调 / 烟测：
    # MariaDB 12 开发 / 联调 / 烟测：
    #
    #
    # depends_on:
    # depends_on:
    #   mysql:
    #   mysql:
    #     condition: service_healthy
    #     condition: service_healthy
    #   AUTH_COOKIE_SECURE: "true"
    #   AUTH_COOKIE_SECURE: "true"
    #   AUTH_COOKIE_SAME_SITE: Strict
    #   AUTH_COOKIE_SAME_SITE: Strict
    # volumes:
    # volumes:
    #   - "./data-mariadb:/data"
    #   - "./data-mariadb:/data"


    # 无外部缓存：
    # 无外部缓存：
    # 1. 删除 `depends_on.cache`
    # 1. 删除 `depends_on.cache`
    # 2. 把 `REDIS_URL` 改为空字符串
    # 2. 把 `REDIS_URL` 改为空字符串
    #
    #
    # environment:
    # environment:
    #   DATABASE_KIND: postgresql
    #   DATABASE_KIND: postgresql
    #   DATABASE_URL: postgresql://user:pass@postgres:5432/image
    #   DATABASE_URL: postgresql://user:pass@postgres:5432/image
    #   REDIS_URL: ""
    #   REDIS_URL: ""
    #   JWT_SECRET: your-secret-key-change-in-production
    #   JWT_SECRET: your-secret-key-change-in-production
    #   AUTH_COOKIE_SECURE: "false"
    #   AUTH_COOKIE_SECURE: "false"
    #   AUTH_COOKIE_SAME_SITE: Strict
    #   AUTH_COOKIE_SAME_SITE: Strict


  postgres:
  postgres:
    container_name: avenrixa-postgres
    container_name: avenrixa-postgres
    image: ${POSTGRES_IMAGE:-postgres:18}
    image: ${POSTGRES_IMAGE:-postgres:18}
    environment:
    environment:
      POSTGRES_DB: image
      POSTGRES_DB: image
      POSTGRES_USER: user
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_PASSWORD: pass
    volumes:
    volumes:
      - "postgres_data:/var/lib/postgresql"
      - "postgres_data:/var/lib/postgresql"
    healthcheck:
    healthcheck:
      test: pg_isready -U user -d image
      test: pg_isready -U user -d image
      interval: 5s
      interval: 5s
      timeout: 5s
      timeout: 5s
      retries: 5
      retries: 5
    restart: unless-stopped
    restart: unless-stopped


  # MySQL 8.4 开发 / 联调 / 烟测：
  # MySQL 8.4 开发 / 联调 / 烟测：
  #
  #
  # mysql:
  # mysql:
  #   container_name: avenrixa-mysql
  #   container_name: avenrixa-mysql
  #   image: mysql:8.4
  #   image: mysql:8.4
  #   environment:
  #   environment:
  #     MYSQL_DATABASE: image
  #     MYSQL_DATABASE: image
  #     MYSQL_USER: user
  #     MYSQL_USER: user
  #     MYSQL_PASSWORD: pass
  #     MYSQL_ROOT_PASSWORD: rootpass
  #   volumes:
  #     - "mysql_data:/var/lib/mysql"
  #   healthcheck:
  #     test: mysqladmin ping -h 127.0.0.1 -uuser -ppass
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10
  #   restart: unless-stopped

  # MySQL 8.4 长期运维：
  #
  # mysql:
  #   environment:
  #     MARIADB_DATABASE: image
  #     MARIADB_USER: user
  #     MARIADB_PASSWORD: pass
  #     MARIADB_ROOT_PASSWORD: rootpass
  #     MYSQL_DATABASE: image
  #     MYSQL_USER: user
  #     MYSQL_PASSWORD: pass
  #     MYSQL_ROOT_PASSWORD: rootpass
  #   volumes:
  #     - "mariadb_data:/var/lib/mysql"
  #   healthcheck:
  #     test:
  #       - CMD-SHELL
  #       - mariadb-admin ping -h 127.0.0.1 -uuser -ppass || mysqladmin ping -h 127.0.0.1 -uuser -ppass
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10
  #   restart: unless-stopped

  # MariaDB 12 长期运维：
  #
  # mysql:
  #   container_name: avenrixa-mariadb
  #   image: mariadb:12
  #   environment:
  #     MARIADB_DATABASE: image
  #     MARIADB_USER: avenrixa
  #     MARIADB_PASSWORD: replace-with-strong-app-password
  #     MARIADB_ROOT_PASSWORD: replace-with-strong-root-password
  #     MYSQL_DATABASE: image
  #     MYSQL_USER: avenrixa
  #     MYSQL_PASSWORD: replace-with-strong-app-password
  #     MYSQL_ROOT_PASSWORD: replace-with-strong-root-password
  #   volumes:
  #     - "mariadb_data:/var/lib/mysql"
  #   healthcheck:
  #     test:
  #       - CMD-SHELL
  #       - mariadb-admin ping -h 127.0.0.1 -uavenrixa -preplace-with-strong-app-password || mysqladmin ping -h 127.0.0.1 -uavenrixa -preplace-with-strong-app-password
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10
  #   restart: unless-stopped

  cache:
    container_name: avenrixa-cache
    image: redis:8
    healthcheck:
      test: redis-cli ping
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

