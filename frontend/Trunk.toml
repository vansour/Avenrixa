[build]
target = "index.html"
dist = "dist"
public_url = "/"

[watch]
watch = ["src"]

[serve]
address = "127.0.0.1"
port = 8080
no_error_reporting = false
open = false

[clean]
dist = "dist"
cargo = false

[tools]
wasm_bindgen = "0.2"
wasm_opt = ["-Oz"]
